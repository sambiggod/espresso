import * as React from "react";
import './index.less';

const SimulatorPanel: React.FC<any> = (props) => {

    return (
        <React.Fragment>
            <div className="espressoSimulatorPanel">
                模拟器面板
            </div>
        </React.Fragment>
    )
}

export default SimulatorPanel;
